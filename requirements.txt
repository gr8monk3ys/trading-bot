alpaca-py>=0.22.0  # Align with pyproject dependency floor
pandas>=2.0.0
numpy>=1.24.3,<3.0.0  # Pin to 1.x to avoid compatibility issues
python-dotenv>=1.2.2
ta-lib
yfinance
statsmodels>=0.14.0  # research/ only (pairs cointegration); kept pending pairs validation
websockets>=16.1.1  # research/ only (tick data); kept pending pairs validation
aiohttp>=3.14.3
aiosqlite>=0.19.0  # Async SQLite for database persistence
matplotlib>=3.7.0  # main.py backtest --plot

# Web Dashboard
fastapi>=0.141.1
uvicorn[standard]>=0.24.0
jinja2>=3.1.0

# Testing dependencies
pytest>=9.0.3
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Security floors for transitive deps (see pyproject [tool.uv] constraint-dependencies)
requests>=2.33.0
starlette>=1.3.1
urllib3>=2.7.0
msgpack>=1.2.2
idna>=3.15
pygments>=2.20.0
